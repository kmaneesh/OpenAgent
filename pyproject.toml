[build-system]
requires = ["setuptools>=69", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "openagent-dev"
version = "0.1.0"
description = "OpenAgent development workspace."
requires-python = ">=3.11"
dependencies = []
